import React from 'react';

import { User } from '@/types/user';

interface CreateProps {
  user: User;
}

export default function Create({ user }: CreateProps) {
  return (
    <div className="flex gap-4 mt-12 w-full max-w-[1000px]">
      
    </div>
  )
}